import { EnumtonamePipe } from './enumtoname.pipe';

describe('EnumtonamePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumtonamePipe();
    expect(pipe).toBeTruthy();
  });
});
