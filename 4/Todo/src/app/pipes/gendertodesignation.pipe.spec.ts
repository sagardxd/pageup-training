import { GendertodesignationPipe } from './gendertodesignation.pipe';

describe('GendertodesignationPipe', () => {
  it('create an instance', () => {
    const pipe = new GendertodesignationPipe();
    expect(pipe).toBeTruthy();
  });
});
